import ProjectDescription
import ProjectDescriptionHelpers

let project: Project =
    .staticFramework(name: "HangulClockTable",
                     platform: .iOS,
                     dependencies: [])
