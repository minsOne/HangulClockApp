import ProjectDescription
import ProjectDescriptionHelpers

let project: Project =
    .staticFramework(name: "FoundationExtension",
                     platform: .iOS,
                     dependencies: [])
